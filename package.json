{
  "name": "@fkn/proxy-origins",
  "version": "0.0.0",
  "main": "build/index.js",
  "type": "module",
  "scripts": {
    "build": "tsc && esbuild --bundle --outfile=build/index.js --format=esm --platform=node src/index.ts"
  },
  "devDependencies": {
    "esbuild": "^0.19.3",
    "typescript": "^5.2.2"
  }
}
